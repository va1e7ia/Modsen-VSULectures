const squaredArray = (arr) => {
  return arr.map((el) => el * 2);
};

const arr = [1, 2, 3, 4, 5];

console.log(squaredArray(arr));

// Создайте стрелочную функцию, которая принимает массив чисел и возвращает новый
// массив, где все числа умножены на 2.
