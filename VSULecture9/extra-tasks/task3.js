const stringLengthArr = (strArr) => {
  return strArr.map((el) => el.length);
};

const arr = ["hello", "hi"];

console.log(stringLengthArr(arr));

// Создайте стрелочную функцию, которая принимает массив строк и возвращает массив их длин.
